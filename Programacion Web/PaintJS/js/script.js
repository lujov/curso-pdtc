function crearElementos () {
    var newDiv = document.createElement("div"); 
    newDiv.classList.add("pixel");
    let contenedor = document.querySelector ("contenedor")
    contenedor.appendChild(newDiv)
}

function agregarElementos(params) {
    
}